# log-analyzer
A cybersecurity learning project - a Python tool that reads Linux auth logs, detects SSH brute force attempts using regex and pandas, and generates a color-coded terminal report. Built to understand real-world log analysis and threat detection
